# ===================== 1. Python通用规则（必选） =====================
# 编译后的字节码（树莓派运行Python会自动生成）
__pycache__/
*.py[cod]
*$py.class
*.so
# 虚拟环境（本地依赖，无需共享）
venv/
env/
.venv/
env/
# 测试/缓存文件
.pytest_cache/
.coverage
htmlcov/
# 打包相关
*.egg-info/
dist/
build/
# ===================== 2. 树莓派/蓝牙开发专属（必加） =====================
# 蓝牙扫描产生的日志/数据文件（本地保留即可）
*.log
*.log.*
*.db
*.csv
*.json
data/
cache/
# 树莓派系统临时文件
*.swp  # Vim编辑临时文件
*~     # 备份文件
.DS_Store  # Mac系统文件（如果从Mac传文件到树莓派会有）
# 蓝牙/硬件配置文件（含本地敏感信息，如MAC地址、串口配置）
*.conf
*.ini
!config_template.ini  # 例外：配置模板文件（不含敏感信息）需要提交
# ===================== 3. 编辑器/SSH开发专属（可选） =====================
# VSCode配置（本地开发设置，无需共享）
.vscode/
.idea/
# SSH终端历史
.history/